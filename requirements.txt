# Data acquisition
sec-edgar-downloader

# RAG framework
langchain
langchain-anthropic
langchain-community

# Vector store (local, no server needed)
chromadb

# Embeddings (free, runs locally)
sentence-transformers

# LLM (Google Gemini, free tier)
google-genai

# Dashboard
streamlit

# Evaluation — ragas 0.4.x is incompatible with langchain 1.x, so evaluate.py
# implements faithfulness + context precision directly via the Gemini judge (google-genai above)

# Utilities
python-dotenv
pandas
beautifulsoup4
